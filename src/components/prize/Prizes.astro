---
import PrizeItem from "./PrizeItem.astro";
import { getLangFromUrl, useTranslations } from '../../i18n/utils';
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<ul class="w-full flex flex-col gap-4 md:flex-row md:gap-48 justify-center items-center lg:items-start mt-8">
    <PrizeItem
    title={t('prize.students.title')}
    prices={[
        t('prize.students.prices1'),
        t('prize.students.prices2'),
        t('prize.students.prices3')
    ]}
    number="🏆"
/>
<PrizeItem
    title={t('prize.teachers.title')}
    prices={[
        t('prize.teachers.prices1')
    ]}
    number="🏆"
/>
</ul>
