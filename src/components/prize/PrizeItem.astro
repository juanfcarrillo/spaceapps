---
interface Props {
    title: string;
    description?: string;
    number: string;
    prices?: string[];
    isFirst?: boolean;
}

const { title, description, number, isFirst, prices } = Astro.props;
---

<li class={`flex flex-col gap-4 w-full md:w-auto shadow-xl px-6 py-8 rounded-lg`}>
    <span class="text-center text-ns-gray-dark text-6xl">{number}</span>
    <h5 class="text-center text-xl font-bold">{title}</h5>
    {
        prices
            ? prices.map((price) => <p class="text-sm"><span class="text-2xl">•</span> {price}</p>)
            : <p set:html={description}></p> 
    }
</li>
