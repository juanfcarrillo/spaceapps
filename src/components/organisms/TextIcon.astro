---
export interface Props {
    icon: string;
}

const { icon } = Astro.props;
---

<div class="flex items-center gap-4 flex-1">
    <span class="text-6xl font-bold">{icon}</span>
    <!-- <img class="w-14 font-bold" src={icon}/> -->
    <p class="text-sm">
        <slot />
    </p>
</div>